diff --git a/collection.go b/collection.go
index 4d89290..1cc9798 100644
--- a/collection.go
+++ b/collection.go
@@ -94,2 +94,5 @@ func CreateAndOpen(path string, schema *CollectionSchema, options *CollectionOpt
-	cPath := C.CString(path)
-	defer C.free(unsafe.Pointer(cPath))
+	cPath, freePath, err := cStringPath(path)
+	if err != nil {
+		return nil, err
+	}
+	defer freePath()
@@ -103,2 +106 @@ func CreateAndOpen(path string, schema *CollectionSchema, options *CollectionOpt
-	err := toError(C.zvec_collection_create_and_open(cPath, schema.handle, cOptions, &cCollection))
-	if err != nil {
+	if err := toError(C.zvec_collection_create_and_open(cPath, schema.handle, cOptions, &cCollection)); err != nil {
@@ -113,2 +115,5 @@ func Open(path string, options *CollectionOptions) (*Collection, error) {
-	cPath := C.CString(path)
-	defer C.free(unsafe.Pointer(cPath))
+	cPath, freePath, err := cStringPath(path)
+	if err != nil {
+		return nil, err
+	}
+	defer freePath()
@@ -122,2 +127 @@ func Open(path string, options *CollectionOptions) (*Collection, error) {
-	err := toError(C.zvec_collection_open(cPath, cOptions, &cCollection))
-	if err != nil {
+	if err := toError(C.zvec_collection_open(cPath, cOptions, &cCollection)); err != nil {
